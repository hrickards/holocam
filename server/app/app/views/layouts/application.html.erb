<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<title><%= t 'title' %></title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>

	<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
	<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
	<!--[if lt IE 9]>
		<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
		<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
	<![endif]-->

	<!-- Cross-site scripting prevention -->
  <%= csrf_meta_tags %>

	<!-- A meta tag indicating whether or not the user is signed in -->
	<!-- Should only be used for UI rendering purposes, nothing important - everything MUST -->
	<!-- of course be checked server-side -->
	<meta name="signed_in" content="<%= signed_in? %>">

	<!-- Set correct locale for internationalisation -->
	<script type="text/javascript">
		I18n.defaultLocale = "<%= I18n.default_locale %>";
		I18n.locale = "<%= I18n.locale %>";
	</script>
</head>
<body>

	<%= render 'navbar' %>

<div class="container" id="main_container">
	<%# Flash messages %>
	<%# If you change this, you should probably change the flash messages manually generated >%
	<%# in queue.js! %>
  <% if flash[:alert] %>
		<div class="alert alert-danger" role="alert">
			<span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
			<span class="sr-only"><%= t 'error.error' %>:</span>
			 <a class="close" data-dismiss="alert">×</a>
			<%= flash[:alert] %>
		</div>
  <% end %>
  <% if flash[:notice] %>
		<div class="alert alert-info" role="alert">
		 <a class="close" data-dismiss="alert">×</a>
			<%= flash[:notice] %>
		</div>
  <% end %>

	<%= yield %>
</div>

</body>
</html>
